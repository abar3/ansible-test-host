---                                             
- name: test 
  hosts: all                             
  remote_user: "ubuntu"                                             
  tasks:                                        
    - name: test my_pluging 
      debug: msg={{ lookup('tss', 3489) }} 